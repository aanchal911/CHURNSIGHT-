# Backend (set on Render dashboard or render.yaml)
# Comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=https://churnsight.vercel.app

# Frontend (Vercel reads VITE_ prefixed vars automatically)
# Not needed — frontend uses relative /api paths proxied by Vercel
